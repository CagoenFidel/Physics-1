[project]
name = "manim-fisica"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Marco", email = "limi03547@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "manim>=0.21.0",
]

[project.scripts]
manim-fisica = "manim_fisica:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
